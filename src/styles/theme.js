import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#80ff80',
    },
    secondary: {
      main: '#8080ff',
    },
  },

  typography: {
    allVariants: {
      color: '#3e5055',
    },
  },
})
